[project]
name = "wy-core"
version = "0.1.1"
description = "Wiley Agent core: provider, tool, context, agent loop, message, log."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
